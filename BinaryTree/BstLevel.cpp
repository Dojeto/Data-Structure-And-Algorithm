  /* void printLevel(TreeNode* root,int level)
    {
        if(root == NULL)
            return;
        if(level == 1)
        {
            cout << root->val;
        }
        else
        {
            printLevel(root->left,level-1);
            printLevel(root->right,level-1);
        }
    } */
